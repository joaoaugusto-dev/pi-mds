// Este arquivo foi mantido como stub por compatibilidade.
// A tabela `preferencias_tags` e suas DAOs foram removidas: preferências
// agora residem diretamente na tabela `funcionarios` (colunas temp_preferida e lumi_preferida).

class PreferenciaTagDaoDeprecated {
  PreferenciaTagDaoDeprecated() {
    throw UnsupportedError(
      'PreferenciaTagDao foi removida. Use Funcionario/FuncionarioService.',
    );
  }
}
