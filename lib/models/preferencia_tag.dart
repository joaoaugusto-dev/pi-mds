// Modelo PreferenciaTag removido — preferências agora residem em `funcionarios`.
@deprecated
class PreferenciaTagStub {
  PreferenciaTagStub() {
    throw UnsupportedError(
      'PreferenciaTag foi removida. Use Funcionario/FuncionarioService.',
    );
  }
}
